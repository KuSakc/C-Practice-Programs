#include <stdio.h>
#include <conio.h>

int main()
{
    int number;

    printf("Number: ");scanf("%d", &number);

        int reverse=0,temp;
    for (int i=1;   ; number/10)
    {
        reverse = reverse *10 + (number -(number/10)*10)
        
    }
}